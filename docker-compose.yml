version: "3.9"
services:
  chip8:
    build: .
    image: chip8-service:latest
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - STORE_DIR=/data/store
    volumes:
      - chip8-data:/data
    restart: unless-stopped
volumes:
  chip8-data:
